import React from 'react';

const Header = () => {
  return (
    <header className="sticky z-100 top-0 h-(--header-height) px-(--side-padding) bg-blue-500">
      <div>Header</div>
    </header>
  );
};

export default Header;
